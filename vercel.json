{
    "buildCommand": "sh vercel.sh && cp -r public .next/server/public",
    "routes": [
        {
            "src": "/public/(.*)",
            "dest": ".next/server/public/public/$1"
        }
    ]
}